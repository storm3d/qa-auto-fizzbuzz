import { test, expect } from '@playwright/test';

test.describe('ClockWise FizzBuzz (Playwright)', () => {

  test('renders correct sequence', async ({ page }) => {
    // open page, click Run, add assertions
  });

});